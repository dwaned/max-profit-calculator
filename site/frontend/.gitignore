# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright outputs (regenerated by the reports workflow).
# The /playwright-report/ here is at repo root — the deployed bundle
# lives at site/frontend/public/playwright-report/, which the reports
# workflow regenerates from scratch via `cp -r` and must therefore be
# tracked by git so Render picks it up.
/playwright-report
/test-results

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
